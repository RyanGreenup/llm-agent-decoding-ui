# NOTE no secrets
# Use sops for secrets generally
PORT=3075
RAW_DOC_PATH="./data/Product Disclosure Statement (PDS).docx"
DATABASE_PATH="./data/app.db"
# Session encryption key (min 32 chars). Generate with: openssl rand -base64 32
SESSION_SECRET=""
# Initial admin password (set before first run). If unset, a random password is generated and printed to stdout.
ADMIN_PASSWORD=""
OPENAI_API_KEY=""
